{{ define "main" }}
<div class="flex six">
  <div class="full off-sixth-600 two-third-600" style="padding-top: 10%">
    <article class="card">

      <header>
        <h1 class="event-title" itemprop="name">{{.Title}}</h1>
      </header>
      <footer>





        {{ $endtime := partial "func/enddate" . }}
        {{ $endtime := $endtime|time}}



        {{if $endtime.Before now }}
          <div class="accent2_color" style="font-style: italic">This event has concluded.</div>
        {{ end }}
        {{ partial "eventtombstone" .}}
        <div class="flex two" style="padding-top: 15px">
          {{ range .Params.venues }}
            <div class="full half-1000">
              {{ with .url }}<a href="{{.|safeHTML}}" target="_blank" class="button">{{end}}Event URL{{with .url}}</a>{{end}}<br/>

            </div>
          {{ end }}
        </div>










        <p class="">{{.Content}}</p>



      </footer>
    </article>

  </div>

</div>











{{ end }}
